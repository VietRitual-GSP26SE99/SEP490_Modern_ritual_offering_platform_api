# Backend API base URL for Vite (must start with VITE_)
# Example for local dev with reverse proxy
VITE_API_BASE_URL=/api

# Example for production (uncomment and adjust if needed)
# VITE_API_BASE_URL=https://vietritual.click/api
